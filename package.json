{
  "private": true,
  "license": "MIT",
  "name": "chrsep.dev",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "scripts": {
    "build": "vp run -r --cache build",
    "check": "vp run -w --cache check:tooling && vp run -w --cache check:svelte",
    "dev": "vp run -r --parallel dev",
    "format": "vp fmt . --write",
    "lint": "vp lint",
    "test": "vp run -w --cache test:unit && vp run -w --cache test:seo"
  }
}
